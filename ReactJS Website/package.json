{
  "name": "modernsnake-server",
  "version": "1.0.0",
  "description": "Serveur pour ModernSNAKE",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "cd react-website && npm install && npm run build && mv build ../",
    "dev": "nodemon server.js",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "socket.io": "^4.7.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.2",
    "gh-pages": "^6.1.1"
  },
  "engines": {
    "node": ">=14.0.0"
  },
  "homepage": "https://[votre-nom-utilisateur].github.io/[nom-du-repo]"
}
